{{#each posts as |post|}}
<div class="row mb-4">
    <div class="col-md-5">
        <h2>
            <a href="/post/{{post.id}}">{{post.title}}</a>
        </h2>
        <p>Written on {{formatDate post.createdAt "MMM DD, YYYY h:mm A"}}</p>
    </div>
   
</div>
{{/each}}